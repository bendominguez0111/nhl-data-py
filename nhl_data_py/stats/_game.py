from nhl_data_py.stats import (
    GAME_BOXSCORE_URL, GAME_CONTENT_URL, GAME_LINESCORE_URL, 
    GAME_LIVE_FEED_URL, GAME_PLAY_TYPES, GAME_STATUS, GAME_TYPES
)

import pandas as pd
import requests

def get_game_boxscore(id: str) -> pd.DataFrame:
    return

def get_game_content(id: str) -> pd.DataFrame:
    return

def get_game_linescore(id: str) -> pd.DataFrame:
    return

def get_game_live_feed(id: str) -> pd.DataFrame:
    return

def get_game_play_types(id: str) -> pd.DataFrame:
    return

def get_game_status(id: str) -> pd.DataFrame:
    return

def get_game_types() -> pd.DataFrame:
    return